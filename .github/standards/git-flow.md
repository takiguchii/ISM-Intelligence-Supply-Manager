# Git Flow

- Protected branches: `main`, `develop`
- Work branches: `feat/`, `fix/`, `mod/`, `css/`, `test/`, `doc/`, `hotfix/`, `refactor/`
- Open PRs into `develop`
- Merge into `main` only for release-ready increments
