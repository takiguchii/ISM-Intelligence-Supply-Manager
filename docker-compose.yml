services:
  mysql:
    image: mysql:8.4
    container_name: ism-mysql-1
    hostname: mysql
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/my.cnf:ro,z
      - ./database/init:/docker-entrypoint-initdb.d:ro,z
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      ism-network:
        aliases:
          - mysql
          - db

  backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
    container_name: ism-backend-1
    hostname: backend
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://0.0.0.0:8080
      DOTNET_USE_POLLING_FILE_WATCHER: "1"
      DOTNET_CLI_HOME: /root
      NUGET_PACKAGES: /root/.nuget/packages
      NUGET_HTTP_CACHE_PATH: /root/.nuget/http-cache
      ConnectionStrings__DefaultConnection: Server=mysql;Port=3306;Database=${MYSQL_DATABASE};User=${MYSQL_USER};Password=${MYSQL_PASSWORD};
      Database__Provider: MySql
    depends_on:
      mysql:
        condition: service_healthy
    links:
      - "mysql:mysql"
      - "mysql:db"
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    volumes:
      - ./backend:/workspace/backend:z
      - nuget_packages:/root/.nuget/packages
    networks:
      ism-network:
        aliases:
          - backend

  frontend:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
    container_name: ism-frontend-1
    hostname: frontend
    environment:
      CHOKIDAR_USEPOLLING: "true"
      NUXT_API_BASE: ${NUXT_API_BASE}
      NUXT_TELEMETRY_DISABLED: "1"
    depends_on:
      - backend
    links:
      - "backend:backend"
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    volumes:
      - ./frontend:/workspace/frontend:z
      - frontend_node_modules:/workspace/frontend/node_modules
      - frontend_nuxt:/workspace/frontend/.nuxt
    networks:
      ism-network:
        aliases:
          - frontend

volumes:
  mysql_data:
  nuget_packages:
  frontend_node_modules:
  frontend_nuxt:

networks:
  ism-network:
    driver: bridge
